/**
 * The website's own models, so the editor and the site can never disagree about the content format.
 * This is the only file that reaches outside the editor folder.
 */
export * from '../../../src/lib/models/content.ts';
export * from '../../../src/lib/models/locale.ts';
export { youtubeId } from '../../../src/lib/models/video.ts';
